# 测试方法

三条通道：**先跑自动测试**（不开游戏，约 1 分钟给出服务端半边的结论），再开游戏做 HUD 手测，
网页包装与版面编辑器有第三条**纯浏览器**通道（第十一章），同样不需要游戏。

> **本轮必须先重启 Minecraft 和服务端。** 协议 **v3**；比赛数据由服务端拥有并下发。
> 模组侧整体重写：**HUD 由版面文档驱动**，新增本机图形服务、网页包装与**可视化版面编辑器**。

```powershell
.\scripts\test-server.ps1 start
```

前置（`mods` 里，版本必须对 1.21.4）：`fabric-api-0.119.4+1.21.4`、`cloth-config-17.0.144-fabric`、
`stevehud-mc1.21.4-0.1.0.jar`，可选 `modmenu-13.0.4`。进服 `127.0.0.1`，`__RATE__` 是管理员。

---

## 一、自动冒烟测试（不需要开游戏）

```powershell
.\scripts\smoke-test.ps1              # 起服 → 探针 → 核对日志 → 停服
.\scripts\smoke-test.ps1 -KeepRunning # 测完不停服，接着做下面的手测
```

| # | 检查 | 预期 |
|---|---|---|
| A1 | `smoke-test.ps1` | 退出码 `0`，输出 `SMOKE TEST: PASS` |
| A2 | 探针断言 | `RESULT: PASS (11 checks)` |
| A3 | 单元测试 | `./gradlew :protocol:test :mod:common:test` → **117 个测试**全过（协议 34 + 共享 83） |
| A4 | 负向对照 | `cd tools/protocol-probe; $env:SKIP_REGISTER=1; node probe.mjs` → **同样 PASS**（断言相反） |
| A5 | 四版本构建 | `./gradlew build` → 四个 mod jar + 一个插件 jar |
| A6 | 版面审计 | `node tools/overlay-audit.mjs --all` → **6 种「预设 × 场景」组合全部 PASS** |
| A7 | 页面探针 | `node tools/web-probe.mjs --url http://127.0.0.1:8788/editor/ --js "document.title"` → 退出码 `0`，不打印任何页面报错 |

A4 的语义别读反：它断言「不注册就收不到」，规则成立时它就通过。

A6 是这一版新增的一条独立通道，值得单独说：它用一个**真实的**无头 Edge/Chrome 加载每一套
预设的每一个场景，然后**量**出来——每个元素的矩形、两两之间有没有重叠、画面窗口和整幅画面的
遮挡比例、哪些文本触发了自适应缩放、缩到下限还在溢出的是哪几条字符串、页面报了什么错。
理由是「看着挺好」不是一条能在下一套预设上依然成立的检查。退出码可直接进 CI；
`--json` 给机器读，默认输出给人读；单跑一套用 `--preset arena --scene compact`。

它需要预览服务器（不需要开游戏）：

```powershell
node tools\web-preview.mjs --port 8788 --preset arena   # 一个终端
node tools\overlay-audit.mjs --all                        # 另一个终端
```

---

## 二、HUD：由版面文档驱动

**这一版的核心变化：HUD 不再写死在代码里，而是读一份版面文档。**

```
config/stevehud/layout.json      ← 版面的唯一真相（首次运行自动生成）
```

三处共用这一份文档，所以它们**不可能各画各的**：

| 渲染器 | 位置 |
|---|---|
| 游戏内 HUD | 每个装了模组的客户端 |
| 网页包装 | OBS 浏览器源 → `http://127.0.0.1:8787/overlay/` |
| 版面编辑器 | `/shude` 或 `http://127.0.0.1:8787/editor/` |

文档里每个元素写的是**锚点 + 偏移 + 尺寸 + 缩放 + 配色 + 动画**，坐标一律是 1920×1080 设计像素，
渲染器按实际视口算一个统一比例。所以换个分辨率、换个 OBS 画布，版面是**等比缩放**而不是“长得差不多”。

比分初始为 0，需要主动给分：`/stevehud score home +1`（第五章）。

### 2.1 十六种元素各就各位

常驻信息层 8 种（B1–B8），数据看板层 8 种（B9–B15，需要一个数据源才出现，
下面用 `/stevehud stat` 喂）。

| # | 检查 | 预期 |
|---|---|---|
| B1 | 进服 | 四角都有内容，不是只有左上角一块 |
| B2 | 左上 比赛角标 | `LIVE` 红点呼吸、墙钟每秒走动、双方短名/头像/队色、大号比分 |
| B3 | 右上 赛事信息 | 赛事名 + 阶段，右对齐 |
| B4 | 右下 计时器 | 全包最大字号；`running` 时下方有呼吸条 |
| B5 | 左下 选手条 | `/stevehud lower3 home` 后从左侧滑入，换方时重播入场 |
| B6 | 底边 跑马灯 | `/stevehud ticker ...` 后通栏匀速滚动；为空时整条收起 |
| B7 | 正中 公告 | `/stevehud announce ...` 后出现，两侧横线向外生长 |
| B8 | 视口安全框 | 四角括号；有跑马灯时下边框上移到跑马灯上沿，读起来是个完整矩形 |
| B9 | `/stevehud stat compare 42 击杀 @home` + `... 37 击杀 @away` | 「数据对比」出现一根从中轴往两边长的条，两侧数字分别是 42 / 37 |
| B10 | `/stevehud stat kills 14 Uzi` … | 「排行榜」按数值从大到小，前 3 名点亮成强调色 |
| B11 | `/stevehud stat totals 79 总击杀` | 「关键指标」出现一格磁贴；再喂 3 条会补满一行 |
| B12 | `/stevehud stat timeline 0 首杀 · Uzi` 之类 | 「事件时间轴」按时间倒序，行首是时间、行中是彩点 |
| B13 | `/stevehud stat series 1 G1` … | 「局分板」逐局列出，底部是总比分；`state=live` 的那局高亮 |
| B14 | 阵容卡 / 对阵卡 / 趋势曲线 | 阵容卡读 `sides[].competitors`；对阵卡读两侧战绩；趋势曲线读 `sides[].series` |
| B15 | `/stevehud stat <任一板> clear` | 那块板**整块收掉**，画面上不留空框 |
| B16 | 编辑器左下「数据」页 | 列出当前所有看板的 key 与前几行内容，照抄即可填进元素的「数据源」 |

### 2.2 锚点缩放（这一版的重点）

元素各自**绕自己那个角**缩放。`config` 里把 `HUD 缩放` 依次调到 25% / 100% / 400%：

| # | 检查 | 预期 |
|---|---|---|
| B9 | 缩放 400% | **左上角的仍在左上角，右上角的仍在右上角**——贴边的那条边不许移动 |
| B10 | 缩放 25% | 同上，四个角各就各位，不是整块往中间缩 |
| B11 | 修改 `layout.json` 里某个元素的 `"scale": 2.0` | 只有它变大，且仍贴着自己那个角 |

B9–B11 是验收线：**如果缩放时元素往画面中间跑，就是这里错了**（整屏 zoom 的典型症状）。

### 2.3 文档就是操作界面

| # | 检查 | 预期 |
|---|---|---|
| B12 | `/shudlayout` | 报出当前版面名、预设名、元素个数、文件路径 |
| B13 | `/shudlayout list` | 列出 `arena / olympia / clean` |
| B14 | `/shudlayout olympia` | **整个服务器的观感当场全换**（金色强调、浅色面板、项目说明字幕）；浏览器包装页同步换 |
| B15 | `/shudlayout clean` | 只剩角标、计时器与两条贴边数据 |
| B16 | 手工改 `layout.json`（比如把某个 `x` 改大 100） | **约 1 秒内自动重载并广播**，不用重启、不用敲命令 |
| B17 | 手工把 `layout.json` 改成非法 JSON | 回落到内置预设继续画，**日志有警告，且你的文件没有被覆盖** |
| B18 | `/shudlayout reload` | 重新从磁盘读取并广播 |

B17 是刻意的：你很可能正改到一半，悄悄回退你的文件比忽略它更糟。

### 2.4 场景与个人偏好

| # | 检查 | 预期 |
|---|---|---|
| B19 | 服务端 `/stevehud scene compact` / `minimal` | 元素按文档里的 `scenes` 增减；场景**由服务端统一控制**，所有客户端一致 |
| B20 | `config` 里关掉「绘制比赛 HUD」 | 游戏内 HUD 消失，**网页包装不受影响** |
| B21 | `config` 里把不透明度调到 30% | HUD 变淡，且是**乘在文档为每个元素设的透明度之上** |
| B22 | 四版本 jar | 每个版本的 HUD 都画同一份文档；观感应有差异（游戏内字体是原版位图字体），但**布局与配色一致** |

B22 说明一下：游戏内用的是原版位图字体，字形和网页不可能一样。**一致的是版面、配色与动画**，
不是像素。这是明确的分工，不是没做完。

---

## 三、设置界面（ClothConfig）

`ClothConfig` 是**前置模组**，不是被打包进 mod 的（`modImplementation` 不打包）。
它只管**客户端个人偏好**——和赛事内容、版面样式无关，那些走版面文档与服务端命令。

| # | 检查 | 预期 |
|---|---|---|
| C1 | `/shudc` 或 `/shudconfig` | 打开设置界面；分类是「游戏内 HUD / 服务端连接 / 本机图形服务」 |
| C2 | 界面标题与每个选项 | **中文**（`zh_cn.json` 里全部有键）；鼠标悬停有说明 |
| C3 | 关掉「绘制比赛 HUD」 | HUD 立即消失，重启后仍是关的 |
| C4 | 「HUD 缩放」拉到 400% | 元素各绕自己的角放大，**贴边的那条边不动**（与第二章 B9 同一回事） |
| C5 | 「HUD 不透明度」拉到 30% | HUD 变淡，且是乘在文档为每个元素设的透明度之上 |
| C6 | 关掉「在本机提供包装页面」 | 图形服务停止；`/shudw` 会提示服务未运行 |
| C7 | 改端口（如 8899） | 服务自动重启到新端口；旧地址失效 |
| C8 | 关掉「只接受本机连接」 | 会监听 `0.0.0.0`——**正式转播不要关**，这会把比赛数据暴露在局域网里 |
| C9 | 从 ModMenu 进设置 | 也能打开（若装了 modmenu） |

C3–C5 只影响**你自己这一台**。比赛内容与版面样式不在这里，这是有意的分工。

如果 C1 打不开：先看日志有没有 `Opened the SteveHUD settings screen`。有日志而界面空白，
说明是渲染问题而不是命令问题，把那行日志和截图一起记下来。

---

## 四、客户端命令（含简写）

全部可在**任意服务器**上用（客户端命令，不需要权限）。每条都有一个短写。

| # | 命令 | 短写 | 作用 |
|---|---|---|---|
| E1 | `/stevehudconfig` | `/shudc` `/shudconfig` | 打开设置界面 |
| E2 | `/stevehudlink` | `/shudl` `/shudlink` | 报连接状态：服务端实现、协议版本、revision、比分摘要 |
| E3 | `/stevehudeditor` | `/shude` `/shudeditor` | 打开版面编辑器（需要管理员） |
| E4 | `/stevehudweb` | `/shudw` `/shudweb` | 打开网页包装，并报出已连接的浏览器数 |
| E5 | `/stevehudhud` | `/shudh` | 开关游戏内 HUD（等同于设置里的那一项） |
| E6 | `/stevehudlayout` | `/shudlay` `/shudlayout` | 报当前版面：名字、预设、元素个数、文件路径 |
| E7 | `/shudlayout list` | — | 列出可用预设 |
| E8 | `/shudlayout olympia` | — | 切换预设（**立即存盘并广播给所有人**） |
| E9 | `/shudlayout reload` | — | 从磁盘重新读取版面 |
| E10 | 按 `Tab` 补全 | — | 预设名可以补全；命令名本身也有短写补全 |

| # | 检查 | 预期 |
|---|---|---|
| E11 | `/shudl` | 报 `协议 v3`，且 revision 会随后续命令增长 |
| E12 | `/shudlayout` | 三行：版面状态、文件路径、提示 |
| E13 | `/shudlayout nope` | 提示没有这个预设，并列出可用的 |
| E14 | `/shudlayout olympia` 后看别人屏幕 | 观感一致（状态与版面都由服务端/文档统一） |
| E15 | 未连服务端时 `/shudl` | 提示尚未连接，不报错 |

---

## 五、服务端命令（需管理员）

13 个子命令，全部在插件侧，会**广播**给所有客户端与包装页。

| # | 命令 | 作用 |
|---|---|---|
| E16 | `/stevehud status` | 报插件状态、协议版本、连接人数、当前比分 |
| E17 | `/stevehud resend` | 给所有人重发一份完整状态（客户端丢包时用） |
| E18 | `/stevehud config` | 服务端侧的配置项 |
| E19 | `/stevehud scene full\|compact\|minimal` | 切场景（**服务端统一控制**，所有客户端一致） |
| E20 | `/stevehud score home +1` | 改比分；`home`/`away` 或队伍 id |
| E21 | `/stevehud clock 12:30` 或 `clock 750` | 设比赛计时；接受 `mm:ss` 或秒数 |
| E22 | `/stevehud announce 标题 \| 副标题` | 显示公告；`\|` 分隔标题与副标题 |
| E23 | `/stevehud ticker 文字一 \| 文字二` | 设跑马灯内容；`\|` 分隔多条 |
| E24 | `/stevehud lower3 home` | 显示某队选手条；`/stevehud lower3` 隐藏 |
| E25 | `/stevehud event 名称 \| 阶段` | 设赛事名与阶段 |
| E26 | `/stevehud stage ...` | 设赛事阶段 |
| E27 | `/stevehud demo` | **40 秒脚本化演示**：把比分、两侧选手条、两次公告、跑马灯、计时全部依次走一遍 |
| E28 | `/stevehud reset` | 回到初始状态 |
| E29 | `/stevehud layout` | 报全服版面：名字与设定者，或「未设定」 |
| E30 | `/stevehud layout preset olympia` | **设定全服版面**：所有客户端与包装页改画它 |
| E31 | `/stevehud layout clear` | 清除全服版面，各客户端回到本机版面 |

| # | 检查 | 预期 |
|---|---|---|
| E32 | `/stevehud demo` | **一条命令看完整个包装**——最省事的验收方式 |
| E33 | 上面每条命令 | 执行后游戏内 HUD 与浏览器包装**同时**变化 |
| E34 | `/stevehud` 后按 Tab | 每个固定取值都有补全（场景、队伍、子命令、版面包名） |
| E35 | 非管理员执行 | 提示需要管理员权限 |

E19 和第二章 B19 是同一件事：场景属于**服务端**，因为它必须对所有屏幕一致。
E29–E31 属于**全服版面**，同样只有服务端能改——理由见第六章。

---

## 六、权限：两种范围，两条规则

**"改版面"在这个项目里其实是两件事，权限完全不同。** 分清楚这一条，整套权限就自洽了。

| 范围 | 改的是什么 | 谁受影响 | 权限 |
|---|---|---|---|
| **本机版面** | 你这台机器的 `config/stevehud/layout.json` | 只有你这台机器的游戏内 HUD 与 OBS 包装页 | **不需要权限** |
| **全服版面** | 服务端持有的那一份 | 所有客户端 + 所有包装页 | **需要 `stevehud.admin`** |
| 比赛数据 | 比分 / 计时 / 公告 / 跑马灯 / 场景 | 同上 | **需要 `stevehud.admin`** |

理由不是"重要程度"，而是**影响范围**：

- 本机版面改的是**你自己的包装工作**，不影响任何人。把它锁在管理员后面，等于把个人偏好锁起来了。
- 全服版面会改**别人的屏幕**，所以判在能强制的地方——服务端，由 `plugin.yml` 的
  `stevehud.admin` 拦住，而不是客户端自己判断。

**优先级：全服版面 > 本机版面。** 管理员没有设定全服版面时（默认如此），各客户端画自己的。
默认不设定是刻意的：如果插件一上来就强制一份全服版面，本机版面就永远用不上了，
而 OBS 包装恰恰是在本机版面上做的。

| # | 检查 | 预期 |
|---|---|---|
| F1 | 非管理员 `/shude` | **能打开编辑器**（这一版改的就是这条：以前要管理员，那是错的） |
| F2 | 在编辑器里拖动并保存 | 生效：**他自己**的游戏内 HUD 和 OBS 包装页都变了 |
| F3 | 另一个玩家看自己的屏幕 | **没变**——本机版面不影响别人 |
| F4 | 非管理员 `/shudw` `/shudc` `/shudh` `/shudlayout` | 都能用（都是个人偏好） |
| F5 | 管理员 `/stevehud layout preset olympia` | **所有人的 HUD 与包装页当场换成奥林匹亚版面包** |
| F6 | 此时非管理员 `/shudlayout` | 第二行报「当前生效的是**全服版面**（由 X 设定）」 |
| F7 | 此时非管理员改本机版面并保存 | 画面上**看不到变化**——而且命令与编辑器都会明说原因 |
| F8 | 编辑器在全服版面生效时 | 画布上方出现橙色横幅，写明"全服版面生效中，你的改动暂不显示" |
| F9 | 管理员 `/stevehud layout clear` | 所有人回到各自的本机版面 |
| F10 | 全服版面生效时退出再重进服务器 | 版面还在（服务端持有） |
| F11 | 换到另一个没有插件的服务器 | 全服版面**不会**跟过去（掉线时客户端会清掉它） |
| F12 | 非管理员 `/stevehud layout preset olympia` | 被权限拦掉，提示需要管理员 |

F7 与 F8 是这一版专门做的：**不告诉运营"你改的暂时看不到"，他会以为编辑器坏了**，
然后反复拖拽一个其实正常工作的东西。

F2 与 F3 是一对，测的是同一件事的两面：本机版面**生效**且**不外溢**。

## 七、持久化与联机状态

| # | 检查 | 预期 |
|---|---|---|
| G1 | `config/stevehud/layout.json` | 首次运行自动生成，内容是三份内置预设之一 |
| G2 | 在编辑器里改版式并保存 | 该文件内容随之更新（缩进过的 JSON） |
| G3 | 手工编辑该文件 | 约 1 秒内游戏内 HUD 与浏览器包装自动跟随 |
| G4 | 重启游戏 | 版式保持（从该文件读回） |
| G5 | 把该文件改成非法 JSON | 回落到内置预设继续画；**日志有警告，文件不被覆盖** |
| G6 | 删除该文件后重启 | 重新生成默认版式 |
| G7 | `config/stevehud/web/` | 首次运行自动解包出包装页与编辑器 |
| G8 | 断开网络 | 游戏内 HUD 照常（数据来自服务端）；网页包装照常（本机服务） |
| G9 | 退出游戏 | 本机图形服务随之停止（架构建模如此）；重启游戏后页面自动重连 |

G5 是刻意的：你很可能正改到一半，悄悄把你的文件回退掉比忽略它更糟。

---

## 八、失败模式对照表

| 症状 | 大概原因 | 先看哪里 |
|---|---|---|
| 游戏内 HUD 完全不出现 | HUD 被关 / 世界未进入 / 服务端没连上 | 设置里 `hudEnabled`；`/shudl` |
| HUD 出现但只有角标和计时器 | 场景是 `minimal` | `/stevehud scene full` |
| HUD 元素位置奇怪、往中间聚 | 缩放算错（整屏 zoom 的典型症状） | 见第二章 B9；确认 `--k`/锚点逻辑 |
| 元素宽度对但字很小/很大 | 版面里该元素的 `width` 与参考宽差距大 | 编辑器里改回接近参考宽，或调 `scale` |
| 文字溢出面板 | 队名过长 | 这是 `.clip` 在工作（截断加省略号）；在编辑器里把框拖宽 |
| 浏览器包装 404 | 本机服务没起来 | `/shudw`；设置里的端口；日志里的 `Could not bind port` |
| 包装页显示「包装页已断线」 | SSE 断了 | 会自动重连；持续不断说明服务没起来 |
| 包装页显示「赛事数据未连接」 | 模组收到过状态但服务端断了 | 包装页自身还活着，问题在模组↔插件那条链路 |
| 包装页显示「本地预览数据」 | 编辑器里开过预览 | 编辑器里点「清除预览数据」 |
| 编辑器改不动画面 | 忘了点保存 | 顶部状态会显示「有未保存改动」 |
| 编辑器不覆盖我的改动 | 正常：服务端版面被别人改了 | 日志会说明；点「放弃改动」可载入服务端那份 |
| 启动时报 `LevelStorageSource` 错误 | 上一个服务端没真正退出，还占着 `session.lock` | `.\scripts\test-server.ps1 stop` 再 start |
| 服务端日志中文乱码 | PowerShell 按 ANSI 解码 UTF-8 | 脚本已用 `-Encoding UTF8`；手工看日志也要加 |

---

## 九、当前版本**尚未实现**、因此无法测试的部分

如实列出，免得把"没做"误判成"做坏了"。

| 项 | 状态 |
|---|---|
| **真正的自定义字体字形** | 未做。游戏内用的是原版位图字体 + 描边/字距，网页用的是系统字体栈。**版面、配色、动画一致，字形不一致** |
| **快捷键（keybind）** | 未做。开关 HUD 目前靠命令或设置界面 |
| **从模组内编辑选手档案** | 未做。档案目前只能走服务端命令推数据 |
| **CSV / 数据库导出** | 未做。插件侧只有内存态 |
| **OBS WebSocket 客户端** | 未做。目前靠 OBS 浏览器源读本机服务；推送到 OBS 文本源还没接 |
| **记分板自动采集** | 未做。目前是命令/API 推送 |
| **插件侧持久化** | 未做。重启服务端后比赛数据回到初始值；版式在客户端侧持久化 |
| **图形服务随游戏常驻** | 不适用：图形服务在模组侧，游戏退出即停。这是已确认的架构选择 |
| **独立 JDK 21** | 未装。当前依赖 Gradle 的 toolchain 自动下载（`~/.gradle/jdks`） |

---

---

## 十、实时更新（链路是不是活的）

前面几章验证的都是"进服那一刻"的状态。这一章验证**服务端主动发一条新消息，客户端的内部状态
和画面跟着变**——不重开游戏、不重连。

现在能主动触发的推送有很多了（第五章的每一条命令），其中最干净的是 `/stevehud resend`：
重发一次 greeting，revision 前进一格。这一章测的是**链路活性**，其中的 `rev` 前进是
"消息真的走完了全链路"的证据。

判读要靠三个**相互独立**的观察点。任何一点不动，都能定位到断在哪一段：

| 观察点 | 在哪看 | 它证明了什么 |
|---|---|---|
| HUD 上的 `rev N` | F2 截图 | 消息走到了渲染层（画面）
| 客户端日志的 greeting 行 | `logs/latest.log` | 消息到了客户端并被解析
| 服务端日志的 `Greeting <玩家>` | `.\scripts\test-server.ps1 logs` | 服务端确实发了

### 怎么按键、怎么看图

游戏窗口不暴露无障碍元素，broker 也把捕获到的栅格传不回来，所以输入和观察都得绕开它：

```powershell
# 发按键（~ 是 SendKeys 记法里的 Enter）
powershell -NoProfile -File .\tools\send-to-window.ps1 -TitlePart Minecraft -Keys "/stevehud resend~"

# 存图：F2，PNG 落在 <游戏目录>/screenshots/
powershell -NoProfile -File .\tools\send-to-window.ps1 -TitlePart Minecraft -Keys "{F2}"
```

关键点：**F2 存出来的 PNG 是一个普通文件**，可以直接打开对比——这条路绕开了"broker 读不到画面"，
是唯一能验证 HUD 的手段。

**脚本是盲发的**，所以每一步都要挑一个效果落在别处（文件、日志）的动作来确认输入真的到了。

### 清单

| # | 操作 | 预期 |
|---|---|---|
| H1 | 进服后 F2 存图，看 HUD 的 `rev N` | 是个具体数字，不是占位 |
| H2 | 发 `/stevehud resend` | 聊天栏出现新的 `[SteveHUD] … 已连接` |
| H3 | 再 F2 存图 | `rev` 变成 `N+1`，其余画面不变 |
| H4 | 客户端日志 | 多一行 greeting；没有 `Dropped a malformed message`、没有版本告警 |
| H5 | 服务端日志 | 多一行 `Greeting <玩家>` |
| H6 | 连发几次 `/stevehud resend` | `rev` 连续递增，不跳号、不重号 |

H6 是真正有信息量的一项：`rev` 应当**单调递增**。跳号说明有消息丢了（分包重组的问题），
重号说明 revision 的分配有并发问题——这两种都比"没反应"更值得追。

H2 若聊天栏毫无动静，先分清是**输入没到**还是**链路坏了**：看服务端日志有没有新的 `Greeting`。
没有 → 按键没进游戏（脚本会打印 `focused: True/False`，先查这个，确认窗口没被别的窗口抢走焦点）；
有 → 输入到了、是链路问题。

`/stevehud resend` 需要管理员（见第六章）。

### 这一章现在能测到的更多了

上面那段原文写的是"内容随比赛数据变化测不了"——**现在能测了**：比赛数据已由服务端拥有，
第五章的每条命令都会推一条新状态下去。最快的做法是 `/stevehud demo`，
40 秒里你会看到比分、公告、选手条、滚动条、计时依次变化，而 `rev` 同时不停前进。

---

## 十一、网页包装与版面编辑器

这一章**不需要启动游戏**（11.1–11.4），也可以走游戏内真实路径（11.5）。

```powershell
node tools\web-preview.mjs
```

打开 **http://127.0.0.1:8788/overlay/**（编辑器在 `/editor/`）。

它直接读源码目录，所以**改页面 → 刷新浏览器 → 看效果**，没有构建、没有复制、不用重开游戏。
版面预设读的是 `protocol/src/main/resources/stevehud/layout/*.json`——**和模组打包进去的是同一份文件**，
不是复制品，所以在这里调好的版面就是游戏里会画出来的版面。

### 11.1 包装页本身

它喂一份预设比赛数据、每秒推送一次，约 44 秒循环演示全部元素。

| # | 检查 | 预期 |
|---|---|---|
| I1 | 打开 `/overlay/` | 透明背景（OBS 里叠在游戏上）、四角安全框、七个元素各就各位 |
| I2 | 左上角标 | `LIVE` 红点呼吸、墙钟每秒走动、队色条、大号比分 |
| I3 | 右下计时器 | 大字；约第 25 秒起计时走动并出现呼吸指示 |
| I4 | 左下选手条 | 约第 2 秒滑入主队卡，第 6 秒换客队，第 25 秒滑出 |
| I5 | 正中公告 | 约第 13 秒出现 `ROUND 3 / 决胜局`，两侧横线向外生长 |
| I6 | 底边跑马灯 | 匀速滚动，接缝处无缝 |
| I7 | 比分变化（约第 10、17 秒） | 数字弹跳 + 闪色 |
| I8 | 浏览器控制台 | 无报错 |
| I9 | 改 `overlay/index.html` 里任一 `calc(N * var(--u))` 的 N，刷新 | 立刻生效 |
| I10 | 把浏览器窗口拉成非 16:9 | 包装**等比缩放**，不拉伸变形 |

`--u` 是画布单位（1 个 `--u` = 1920×1080 画布上的 1 像素）。这就是为什么编辑器里拖到哪，OBS 里就在哪。

### 11.2 编辑器：拖拽与几何

| # | 检查 | 预期 |
|---|---|---|
| I11 | 打开 `/editor/` | 三栏：左预设/图层/预览，中画布，右属性/配色/JSON/日志 |
| I12 | 画布 | 中间是**真实的包装页**（iframe，1920×1080），上面浮着拖拽框 |
| I13 | 单击任一元素 | 出现橙色选中框 + 名称标签 + 右下角缩放手柄；右侧属性面板填上该元素的属性 |
| I14 | 拖动元素 | **画布里的包装页当场跟着动**（不需要保存）；属性面板的 X/Y 同步变化 |
| I15 | 拖右下角手柄 | 宽高变化；`自适应`（0）的元素会把当前尺寸固化成数值 |
| I16 | 方向键 / Shift+方向键 | 每次移动 1 / 10 像素 |
| I17 | `Delete` / `Esc` | 删除选中元素 / 取消选择 |
| I18 | 属性面板改「锚点」 | 元素重新贴到新那个角，偏移值不变 |
| I19 | 拖画布外 | 元素可以挂到画面外，边框变虚线提示 |

I14 的关键是**所见即所改**：画布不是示意图，就是那个会上 OBS 的页面。

### 11.3 编辑器：图层、预设、配色

| # | 检查 | 预期 |
|---|---|---|
| I20 | 图层列表 | 七行，顺序 = 绘制顺序（靠前 = 在后面）；点一行即选中 |
| I21 | 点某行右侧的眼睛 | 该元素隐藏/显示，画布当场变化 |
| I22 | 左上角下拉 + `+` | 可添加任意类型的新元素；同类型已存在时新元素**默认隐藏**并在日志说明 |
| I23 | 点「复制」 | 复制一份并偏移 24 像素，自动选中副本 |
| I24 | 点预设 `奥运转播` | **整个编辑器的主色跟着变金**，画布与图层全换 |
| I25 | 改「全局配色」里的主色 | 画布立即变色；**编辑器自己的强调色也跟着变**（工具跟着被编辑的东西走） |
| I26 | 改「安全边距」 | 画布上的虚线安全框跟着缩放 |

I25 是刻意设计：编辑器的主色取自当前版面包，而不是写死一个跟包装无关的颜色。

### 11.4 编辑器：保存、广播、冲突

| # | 检查 | 预期 |
|---|---|---|
| I27 | 改点什么 | 顶部「未保存改动」，保存按钮变为可用 |
| I28 | 点「保存并广播」 | 按钮变灰、状态转「已同步」，日志出现「已保存并广播」 |
| I29 | **同时开两个浏览器标签**（`/overlay/` 和 `/editor/`） | 在编辑器里保存，**包装页标签不用刷新就跟着变** |
| I30 | 在编辑器里改了但**不保存**，此时用 `curl` 往 `/api/layout` POST 一份别的 | 编辑器**不覆盖你的改动**，日志报「服务端版面被改动了，但本页有未保存改动，未覆盖」 |
| I31 | 断网/停服务（关掉设置里「在本机提供包装页面」） | 状态转红并自动重连；恢复后自动同步 |
| I32 | 带未保存改动关标签页 | 浏览器弹确认框 |
| I33 | 「导出 JSON」/「导入 JSON」 | 导出为文件；导入后画布立即更新 |
| I34 | 「面板 JSON」手改后点「应用」 | 生效；JSON 非法时输入框变红并提示，**不应用** |

I30 和 I28 的 `saveInFlight` 是一对：**自己保存的回声不能被误报成"别人改了版面"**，
否则运营会学会忽略那个唯一不能忽略的警告。

### 11.5 游戏内真实路径

| # | 检查 | 预期 |
|---|---|---|
| I35 | 进服后 `/shudw` | 浏览器打开包装页，**内容与游戏内 HUD 一致**（同一份状态 + 同一份版面） |
| I36 | 在游戏里 `/stevehud score home +1` | 游戏内和浏览器**同时**变化 |
| I37 | `/shude`（**任意玩家**） | 打开编辑器（不需要管理员）；拖动并保存后，**他自己的游戏内 HUD 当场跟着换版** |
| I37b | 全服版面生效时看编辑器顶部 | 状态标签变「全服版面」，画布上方出现橙色横幅说明改动暂不显示 |
| I38 | 页面来源 | 由模组从 `config/stevehud/web/` 提供（首次运行自动解包，**不覆盖已有文件**） |
| I39 | 改 `config/stevehud/web/overlay/index.html` 后刷新浏览器 | 生效；删掉它再重开游戏会被重新解包 |
| I40 | 聊天栏报出的「已连接浏览器数」 | 打开/关闭浏览器标签时随之变化 |
| I41 | 非管理员执行 `/shude` | **照常打开**——编辑器改的是本机版面，不需要权限。需要权限的是 `/stevehud layout`（第六章 F12） |
| I42 | 关掉设置里「在本机提供包装页面」 | 服务停；正在看的页面转断线并自动重连 |

I37 是这一版最值得测的一条：**编辑器 → 服务端 → 所有客户端与包装页**，一整条链路。

### 11.6 界面规范（用 ui-ux-pro-max 定的，可核对）

编辑器的视觉是按本地那个 `ui-ux-pro-max` 技能库定的，方向是 **Brutalism + Minimalist Monochrome**：

| 项 | 结论 | 怎么核 |
|---|---|---|
| 圆角 | **全零**，无例外 | 控制台里遍历 `.editor-page *` 的 `borderRadius`，应无一个非 0 |
| 阴影 | **无**；层次只靠 1px 边框 + 中性色阶 + 反白 | 遍历 `boxShadow`，应全为 `none` |
| 模糊 | 无 | 遍历 `backdropFilter`，应全为 `none` |
| 底色 | **纯黑 `#000000`**，表面是中性灰阶（`#0a0a0a/#141414/#1e1e1e`），**不带蓝** | 取各表面的 `backgroundColor`，蓝通道不应高出红/绿 |
| 对比度 | 正文 ≥ 4.5:1（实测 16.3 / 9.6 / 5.7 / 11.5） | 见 11.4 的核对脚本 |
| 图标 | 内联 SVG，**不用 emoji** | 页面里 `svg` 元素计数；正文不应匹配到 emoji 区间 |
| 焦点 | 每个可聚焦控件都有 2px 硬边框焦点环 | `Tab` 走一遍 |
| 动效 | 约 100ms 的机械感过渡，不是缓入缓出 | — |
| 减少动效 | 响应 `prefers-reduced-motion` | 系统里打开后指示点不再闪 |

两条**有意偏离**技能库建议的地方，理由写在 `shared/stevehud.css` 第七节：

1. **不加载网络字体**。技能库推荐 JetBrains Mono 等，但转播机常离线、项目硬规则是零网络请求。
   字体栈改成**优先本机已安装**的同名字体，缺失则回落平台等宽/系统字体。
2. **保留有含义的颜色**。严格单色会让颜色选择器无法显示颜色。强调色（取自被编辑的版面包）、
   色板、队色、连接状态保留颜色；所有结构性表面、边框、标签、控件一律灰阶。

> 关于纯黑的说明：技能库的「Modern Dark」一条提醒纯黑在 OLED 上会有拖影。这条**被有意推翻**：
> 你明确要求全黑，而且这是桌面监视器上的工具，不是手机。

### 这一章测不到的

- 模组崩溃或游戏退出后，本机图形服务也就停了——图形服务在模组侧，这是已确认的架构选择。
  重启游戏即可恢复；页面会自动重连。
- OBS 里的实际观感（透明背景是否干净、1920×1080 是否对齐）需要在 OBS 里加一次浏览器源才能确认。
  预览页与 OBS 里是同一个文件、同一份版面，所以能在这里确认的，到 OBS 里也一样。
- 游戏内字体是原版位图字体，与网页字体不同；**版面、配色、动画一致，字形不一致**，这是设计分工。
